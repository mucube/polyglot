#include <iostream>

#define True int main() {
#define False }
#define python_code std::string python_code
#define fib(n) long long int fibArray[n]; fibArray[0] = 0; fibArray[1] = 1; for (int i=2; i<n; i++) { fibArray[i] = fibArray[i-1] + fibArray[i-2]; } for (int j=0; j<n; j++) {std::cout << fibArray[j] << " ";} std::cout << "\n";
#define exec(x) fib(50)

True
python_code = "from functools import reduce\nfib = lambda n:reduce(lambda x,n:[x[1],x[0]+x[1]], range(n),[0,1])[0]\nfor n in range(50): print(fib(n), end=' ')";
exec(python_code);
False